import { DateTime } from "ionic-angular";
import { NumberLiteralType } from "typescript";

export interface Workout {
    key?: string;
    rep: number;
    weight:number;
    date: number;
    maxWeight:number;
    name:string;
    
}